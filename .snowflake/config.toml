# =============================================================================
# Snowflake Connection Configuration
# =============================================================================
# 
# Single default connection - database/schema selection is handled by:
#   - Database: Project enum in app/projects/__init__.py
#   - Schema: TARGET env var in app/src/environment.py
#
# Environment Variables:
#   - TARGET: DEV | STAGING | PROD (controls schema)
#   - DEVELOPER: Your name (required for DEV, creates DEV_{DEVELOPER} schema)
# =============================================================================

[connections.default]
account = "vx70256-sw09563"
user = "ML_LAYER_USER"
role = "ML_LAYER_ROLE"
warehouse = "ML_LAYER_WH"
authenticator = "SNOWFLAKE_JWT"
private_key_path = ".keys/rsa_key.p8"
